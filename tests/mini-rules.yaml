clock:
  patterns: ['^clocks$', '-clocks$']
gpio:
  patterns: ['^gpios?$', '-gpios?']
pinctrl:
  patterns: ['^pinctrl-']
  struct: [ref, ref, ref, ref, ref]
rockchip,pins:
  patterns: ['^rockchip,pins$']
  struct: [d, d, d, ref]
